# Copy this file to .env.prod and replace placeholder values before running
# docker compose --env-file .env.prod -f docker-compose.yml -f docker-compose.prod.yml up --build -d

# ============================================
# DATABASE (shared — used by postgres + backend)
# ============================================
POSTGRES_DB=playlocal
POSTGRES_USER=playlocal
POSTGRES_PASSWORD=replace-with-strong-db-password

# ============================================
# BACKEND ONLY (Spring Boot — Render env vars)
# ============================================
BACKEND_PORT=8080

# JWT
JWT_SECRET=replace-with-strong-jwt-secret-at-least-32-chars
JWT_EXPIRATION=86400000

# Object Storage (MinIO / S3)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=replace-with-strong-minio-password
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=replace-with-strong-minio-password
S3_BUCKET_NAME=uploads
AWS_REGION=us-east-1
S3_ENDPOINT=http://minio:9000
S3_PUBLIC_ENDPOINT=http://localhost:9000
S3_FORCE_PATH_STYLE=true

# Resend Email (https://resend.com/api-keys)
# For testing without a domain, use: onboarding@resend.dev (sends only to your signup email)
# For production, verify your domain at https://resend.com/domains
RESEND_API_KEY=re_replace_with_your_api_key
RESEND_FROM_EMAIL=onboarding@resend.dev
RESEND_FROM_NAME=PlayLocal
RESEND_ENABLED=true

# reCAPTCHA v3 — server-side verification (https://www.google.com/recaptcha/admin)
# NEVER expose this key to the frontend
RECAPTCHA_SECRET_KEY=replace-with-your-recaptcha-secret-key
RECAPTCHA_ENABLED=true
RECAPTCHA_SCORE_THRESHOLD=0.5