# name: Docker Build and Test

# on:
#   push:
#     branches: [main, dev, feature/*]
#   pull_request:
#     branches: [main, dev]

# env:
#   REGISTRY: ghcr.io
#   # IMAGE_NAME is now set in a step

# jobs:
#   build-services:
#     runs-on: ubuntu-latest
#     timeout-minutes: 20
#     strategy:
#       matrix:
#         service:
#           [
#             decomposition_service,
#             parser_service,
#             completion_tracker_service,
#             scraper_service,
#           ]

#     name: Build ${{ matrix.service }}

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set image name
#         id: set_image_name
#         run: echo "IMAGE_NAME=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Log in to Container Registry
#         if: github.event_name != 'pull_request'
#         uses: docker/login-action@v3
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Extract metadata
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/${{ matrix.service }}
#           tags: |
#             type=ref,event=branch
#             type=ref,event=pr
#             type=sha,format=short
#             type=raw,value=latest,enable={{is_default_branch}}

#       - name: Build and push Docker image
#         uses: docker/build-push-action@v5
#         with:
#           context: PermitParser-Application
#           file: PermitParser-Application/src/services/${{ matrix.service }}/Dockerfile
#           push: ${{ github.event_name != 'pull_request' }}
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
#           platforms: linux/amd64
#           build-args: |
#             BUILDKIT_INLINE_CACHE=1

#       - name: Test Docker image locally
#         if: github.event_name == 'pull_request'
#         run: |
#           # Build image locally for testing (without pushing) - only for PRs
#           docker build -t test-${{ matrix.service }}:latest \
#             -f PermitParser-Application/src/services/${{ matrix.service }}/Dockerfile \
#             PermitParser-Application

#           # Test the locally built image
#           # For scraper_service (CLI tool), override entrypoint for test
#           # For other services (web servers), run python directly
#           if [ "${{ matrix.service }}" = "scraper_service" ]; then
#             docker run --rm --entrypoint python3 test-${{ matrix.service }}:latest \
#               -c "import sys; print('Python version:', sys.version); print('Service: ${{ matrix.service }} - Build successful!')"
#           else
#             docker run --rm test-${{ matrix.service }}:latest \
#               python -c "import sys; print('Python version:', sys.version); print('Service: ${{ matrix.service }} - Build successful!')"
#           fi
